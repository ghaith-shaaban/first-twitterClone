
    <div class="card">
        <div class="card-body">
            <h4> <span class="{{$icon ?? 'fas fa-users'}}"></span>{{$title ?? ''}}</h4>
            <p class="fs-2">{{$data ?? 0}}</p>
        </div>
    </div>

